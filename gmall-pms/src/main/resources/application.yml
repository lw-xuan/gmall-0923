server:
  port: 18081
feign:
  sentinel:
    enabled: true
mybatis-plus:
  mapper-locations: classpath:mapper/pms/**/*.xml
  type-aliases-package: com.atguigu.gmall.pms.entity
  global-config:
    db-config:
      id-type: auto
spring:
  cloud:
    alibaba:
      seata:
        tx-service-group: tx-gmall-group
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.171.128:3306/guli_pms
    username: root
    password: root
  rabbitmq:
    host: 192.168.171.128
    port: 5672
    virtual-host: /lwx
    username: lwx123
    password: lwx123
    publisher-returns: true
    publisher-confirm-type: correlated